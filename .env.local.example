# ============================================================
# Nova26 — Environment Variables
# ============================================================
# Copy this file to .env.local and fill in your values.
# Never commit .env.local to source control.
# ============================================================

# ── Convex ──────────────────────────────────────────────────
# Required: your Convex deployment URL
# Get from: https://dashboard.convex.dev → your project
NEXT_PUBLIC_CONVEX_URL=https://your-project.convex.cloud

# Used server-side for the ConvexBridge (no NEXT_PUBLIC_ needed)
CONVEX_URL=https://your-project.convex.cloud

# Convex deploy key (only needed for CI/CD, not development)
# CONVEX_DEPLOY_KEY=prod:...

# ── Auth (Convex Auth) ───────────────────────────────────────
# JWKS endpoint for token validation (set automatically by Convex Auth)
# CONVEX_AUTH_PRIVATE_KEY=...

# Optional: GitHub OAuth provider
# Register at: https://github.com/settings/applications/new
# Callback URL: https://your-project.convex.cloud/api/auth/callback/github
# GITHUB_CLIENT_ID=Ov23li...
# GITHUB_CLIENT_SECRET=your-github-secret

# ── LLM Providers ───────────────────────────────────────────
# OpenAI (for GPT models)
# OPENAI_API_KEY=sk-...

# Anthropic (for Claude models)
# ANTHROPIC_API_KEY=sk-ant-...

# Ollama (local models — default endpoint)
# OLLAMA_BASE_URL=http://localhost:11434

# FAL.ai (image generation)
# FAL_KEY=...

# ── Langfuse (Observability) ─────────────────────────────────
# Get from: https://langfuse.com
# LANGFUSE_PUBLIC_KEY=pk-lf-...
# LANGFUSE_SECRET_KEY=sk-lf-...
# LANGFUSE_BASEURL=https://cloud.langfuse.com

# ── Redis / BullMQ ───────────────────────────────────────────
# REDIS_URL=redis://localhost:6379

# ── Nova26 CLI flags ─────────────────────────────────────────
# Set to "true" to enable verbose Convex sync logging
# NOVA26_VERBOSE=false

# Enable real-time Convex cloud dashboard sync in the CLI
# NOVA26_CONVEX_SYNC=true

# ── Next.js ──────────────────────────────────────────────────
# Only needed for custom domains in production
# NEXT_PUBLIC_APP_URL=https://nova26.app
